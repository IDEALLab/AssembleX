"""Core domain logic for AssembleX.

Holds the assembly model, planning, simulation, rendering, tool analysis, and
manual/feedback generation. The CLI entry point (main.py), the subcommand
handlers (run_*.py), and settings.py live at the repository root.
"""
